#include <stdio.h>



int str_len(char[20] string1){
    int count;
    while string1[count] != 
}

int main(){
    float radius;
    float result;
    printf("Enter the radius of the sphere\n");
    scanf("%f",&radius);
    result = volume(radius);
    printf("The volume of this circle is %f\n",result);
}